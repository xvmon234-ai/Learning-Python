total_sum = 0

for num in range(10):
  total_sum += num

print(f"합계 : {total_sum}")

#range나 슬라이싱할 때 끝자리는 포함하지 않는다는 사실을 까먹음



"""
[학습 기록]

- 학습 질문:
  - 'range(10)'으로 코드를 작성했는데, 결과가 1부터 10까지의 합계인 55가 아닌 45가 나왔음.
  - 문제에서 요구한 '1부터 10까지'의 합계와 실제 코드의 결과가 달랐음.

- 문제 해결:
  - 'range()' 함수의 동작 방식을 다시 확인했음. 'range(N)'은 0부터 N-1까지의 숫자를 생성한다는 것을 알게 되었음.
  - 따라서 1부터 10까지의 숫자를 포함하려면 'range(1, 11)'로 시작 값과 끝 값을 명시해야 함을 깨달았음.
  - 코드를 'for num in range(1, 11):'으로 수정하여 문제를 해결했음.

- 추가 학습:
  - **range() 함수의 정확한 범위 이해 (Further Study 필요)**:
    'range(stop)', 'range(start, stop)', 'range(start, stop, step)' 세 가지 사용법에서 'stop' 값은 항상 **미만(exclusive)**이라는 점을 명확히 기억해야 함. 이는 반복문에서 원하는 범위를 정확히 지정하는 데 매우 중요함.
  - 파이썬의 내장 함수 `sum()`을 사용하면 `sum(range(1, 11))`처럼 더 간결하게 합계를 계산할 수 있다는 것을 알게 되었음.
"""
