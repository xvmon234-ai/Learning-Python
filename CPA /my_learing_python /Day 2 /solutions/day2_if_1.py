# ==============================================================================
# Day 2: [문제 1-1] 홀수/짝수 판별기 (Day 2 학습 내용)
# ==============================================================================

# --- [사전 준비] ---
# 사용자 입력이 필요한 경우, 주석 처리된 input() 함수를 해제하고 사용하세요.
# print() 문을 통해 결과를 확인하세요.


# --- [최초 나의 코딩] ---
 number = int(input("숫자를 입력하세요: "))

 if number % 2 == 0:
     print("해당 숫자는 짝수입니다.")
 else:
     print("해당 숫자는 홀수입니다.") # 최초에 else 들여쓰기를 하지 않아서 else가 if절에 포함되는 오류 발생 > 들여쓰기에 대한 추가 학습

# --- [코드 실행 결과] ---
"""
(예시)
숫자를 입력하세요: 7
해당 숫수는 홀수입니다.

숫자를 입력하세요: 4
해당 숫자는 짝수입니다.
"""

# --- [피드백] ---
"""
문제 1-1을 아주 훌륭하게 해결했음. `number = int(input("숫자를 입력하세요: "))` 코드를 통해 사용자로부터 숫자를 받아 `number` 변수에 정수형으로 잘 저장했고, `if number % 2 == 0:`을 사용해 짝수 여부를 정확히 판별하고, 그 결과에 따라 적절한 메시지를 출력하도록 로직을 잘 구성했음.

특히, `else` 들여쓰기 오류를 스스로 발견하고 수정했던 점이 매우 인상적임. 이 부분이 바로 파이썬에서 **들여쓰기의 중요성**을 체득하는 과정이었음.
"""

# --- [모범 답안] ---
# 최초 나의 코딩이 이미 모범적으로 해결되었음. (수정된 코드와 동일)

# --- [학습 기록] ---
"""
**학습 질문**: 숫자를 입력받아 홀수인지 짝수인지 판별하는 조건문을 어떻게 작성해야 할까?

**문제 해결**:
1.  **사용자 입력 및 형 변환**: `input()` 함수로 사용자로부터 문자열 입력을 받고, `int()`로 정수형으로 변환하여 `number` 변수에 저장했음.
2.  **홀짝 판별 로직**: **나머지 연산자(`%`)**를 사용하여 `number % 2 == 0` 조건을 통해 짝수를 판별하고, **`if/else` 구문**으로 결과를 출력했음.
3.  **오류 경험 및 해결**: 최초 코딩 시 `else` 문에 들여쓰기를 하지 않아 `IndentationError`가 발생했음. 파이썬은 **들여쓰기로 코드 블록을 구분**한다는 것을 상기하고, `else`를 `if`와 같은 들여쓰기 수준으로 맞춰 문제를 해결했음.

**추가 학습**:
-   **들여쓰기의 중요성 (Further Study 필요)**: 파이썬에서 들여쓰기는 단순한 코딩 스타일이 아니라, 코드의 종속성(scope)을 결정하는 핵심 문법 요소임을 명확히 이해했음. (예: `if` 문 아래의 들여쓰기된 코드는 `if` 조건이 참일 때만 실행됨)
"""
