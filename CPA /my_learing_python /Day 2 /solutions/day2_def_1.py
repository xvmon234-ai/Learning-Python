# ==============================================================================
# Day 2: 파이썬 흐름 제어 및 함수 활용 스터디 기록 - 문제 3-1
# [문제 3-1] 인사말 함수
# ==============================================================================

# --- [사전 준비] ---
# print() 문을 통해 결과를 확인하세요.


# --- [최초 나의 코딩] ---
 def greet(name):
     print(f"안녕하세요, {name}님!") # name이 여기서 매개변수라고 하는데, 해당 내용에 대해서 추가 학습 필요

 greet("강민석")

# --- [코드 실행 결과] ---
"""
(예시)
안녕하세요, 강민석님!
"""

# --- [피드백] ---
"""
`greet` 함수를 정확히 정의하고 호출했음. `name`이 매개변수이고, `"강민석"`이 인수로 전달되어 "안녕하세요, 강민석님!"이 출력되는 것을 확인했음.

그리고 "name이 매개변수라는 개념에 대한 추가 학습이 필요하다"고 스스로 언급한 점이 아주 좋았음. 이 부분이 바로 `further_study.py`에 정리될 중요한 내용이었음.
"""

# --- [모범 답안] ---
# 최초 나의 코딩이 이미 모범적으로 해결되었음.

# --- [학습 기록] ---
"""
**학습 질문**: 이름을 입력받아 인사말을 출력하는 함수를 어떻게 정의하고 호출할까? 함수에서 'name'은 왜 매개변수라고 부를까?

**문제 해결**:
1.  **함수 정의**: `def greet(name):`과 같이 **`def` 키워드**를 사용하여 `greet` 함수를 정의했음.
2.  **매개변수 사용**: 괄호 안의 `name`을 **매개변수(Parameter)**로 지정하여 함수가 외부로부터 이름을 받을 수 있도록 했음.
3.  **함수 호출**: `greet("강민석")`과 같이 함수를 호출하고 `"강민석"`이라는 **인수(Argument)**를 전달하여 매개변수 `name`에 할당되도록 했음.

**추가 학습**:
-   **매개변수와 인수의 명확한 구분 (Further Study 필요)**: '매개변수'는 함수 정의 시 '받을 값의 이름', '인수'는 함수 호출 시 '실제로 전달하는 값'임을 명확히 구분해야 함. 이 두 용어의 개념을 정확히 구분하는 것이 함수를 이해하고 활용하는 데 매우 중요함.
-   **[공인회계사 업무 연관성]**:
    * **보고서 생성 자동화**: 정형화된 보고서나 이메일 양식에서 특정 변수(예: 회사 이름, 보고 기간)만 바꿔가며 반복적으로 문구를 생성할 때 함수를 활용하여 효율성을 높일 수 있음. (예: `generate_report_header(company_name, report_period)`)
    * **코드 재사용성**: 회계 분석 시 자주 사용되는 계산 로직(예: 이자 계산, 감가상각 계산)을 함수로 만들어 두면, 필요할 때마다 호출하여 반복적인 코드 작성을 피하고 오류를 줄일 수 있음. 이는 코드의 유지보수성도 향상시킴.
"""
