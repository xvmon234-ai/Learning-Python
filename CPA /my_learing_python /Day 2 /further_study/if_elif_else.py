#if-elif-else 문의 순차적 조건 검사 원리

[개념 설명]
파이썬의 'if-elif-else' 문은 위에서 아래로 순차적으로 조건을 검사함.
한번이라도 'True'인 조건을 만나 해당 코드 블록을 실행하면,
나머지 'elif'와 'else' 블록은 **절대로 실행되지 않고 건너뛰어짐.**
이러한 특성 때문에, 앞선 조건에서 이미 처리된 범위는 다음 'elif' 조건에서 다시 명시적으로 확인할 필요가 없음.

[예시]
성적 계산기 코드를 통해 이해:

성적: 95점
1. if grade >= 90: (True) -> "A 학점입니다." 출력. 이후 elif, else는 실행되지 않음.

성적: 85점
1. if grade >= 90: (False)
2. elif grade >= 80: (True) -> "B 학점입니다." 출력. 이후 else는 실행되지 않음.

성적: 65점
1. if grade >= 90: (False)
2. elif grade >= 80: (False)
3. elif grade >= 70: (False)
4. else: (실행) -> "F 학점입니다." 출력.

이처럼, 'elif grade >= 80'에 도달했다는 것은 이미 'grade < 90'이라는 것이 전제되어 있다는 의미임.
따라서, 'elif 90 > grade >= 80'과 같은 복합 조건은 논리적으로는 맞지만, 'elif grade >= 80'만으로도 충분하고 더 간결함.

[활용 팁]
이러한 순차적 검사 특성을 이해하면 코드를 더 간결하고 효율적으로 작성할 수 있음.
가장 범위가 넓거나 가장 엄격한 조건을 먼저 배치하는 것이 일반적임.
