# --- Day 2: 반복문 - 반복문 변수 할당 개념 재확인 ---
"""
[주제]
for 반복문에서 변수 할당의 개념

[개념 설명]
파이썬의 `for` 반복문에서 사용되는 반복 변수(loop variable)는
반복이 진행될 때마다 순회 가능한(iterable) 객체(리스트, 문자열, range 등)의
다음 요소를 **새롭게 할당받는(재할당) 개념**입니다.

이는 변수가 단순히 시퀀스의 요소를 '참조'하는 것과는 다릅니다.
매 반복마다 새로운 값이 변수에 '복사'되거나 '바인딩'된다고 이해할 수 있습니다.

[예시]
1.  `for num in range(3):`
    -   첫 번째 반복: `num`에 `0`이 할당됩니다.
    -   두 번째 반복: `num`에 `1`이 할당됩니다.
    -   세 번째 반복: `num`에 `2`가 할당됩니다.

2.  `fruits = ['사과', '바나나']`
    `for fruit in fruits:`
    -   첫 번째 반복: `fruit`에 `'사과'`가 할당됩니다.
    -   두 번째 반복: `fruit`에 `'바나나'`가 할당됩니다.

[활용 팁]
이러한 변수 할당 개념을 이해하면, 반복문 내부에서 변수의 값이 어떻게 변하는지 명확히 파악할 수 있습니다. 특히, 반복문 외부에서 선언된 변수와 반복문 내부에서 재할당되는 변수의 관계를 이해하는 데 중요합니다.
"""
# # 예시 코드 (실제로 실행해 보며 이해)
# my_var = "외부 변수"
# print(f"반복문 시작 전: {my_var}")
#
# for i in range(3):
#     my_var = f"내부 변수 {i}" # 반복될 때마다 my_var에 새로운 값이 할당됨
#     print(f"반복 {i} 중: {my_var}")
#
# print(f"반복문 종료 후: {my_var}") # 마지막으로 할당된 값이 남아있음
