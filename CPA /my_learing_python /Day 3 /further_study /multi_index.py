# ==============================================================================
# [문제 2] 학습 질문 / 문제 해결 / 추가 학습
# ==============================================================================

"""
[학습 질문]
`set_index()` 메서드로 여러 컬럼을 인덱스로 설정할 때, 컬럼 이름을 리스트로 묶지 않으면 오류가 발생하는 이유와
다중 인덱스(MultiIndex)가 어떻게 구성되는지 궁금했음.
"""

"""
[문제 해결]
`df.set_index(["컬럼1", "컬럼2"])`와 같이 컬럼 이름들을 리스트로 묶어 전달함으로써
다중 인덱스를 성공적으로 설정했음.
`df.info()`를 통해 `MultiIndex` 타입으로 변경되었음을 확인했고,
기존 인덱스 컬럼들이 제거되어 인덱스 레벨로 올라간 것을 파악했음.
"""

"""
[추가 학습]
1. MultiIndex의 개념과 구조(`levels`, `codes`, `names` 속성)를 더 깊이 이해해야 함.
2. MultiIndex DataFrame에서 특정 레벨의 인덱스를 기준으로 데이터를 선택하거나 정렬하는 방법을 연습해야 함.
3. `MultiIndex`를 가진 DataFrame에서 `reset_index()`를 사용했을 때의 동작 방식(모든 인덱스 레벨이 컬럼으로 복원되는지 등)을 탐구해야 함.
"""
